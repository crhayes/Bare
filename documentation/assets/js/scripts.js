
$(document).ready(function() {
	
	window.prettyPrint && prettyPrint();

	/**
	 * Include this for Color Box
	 */
	// $('.colorbox').colorbox();	

	/**
	 * Include this to validate a form
	 */
	//$("#form").validate();
	
});